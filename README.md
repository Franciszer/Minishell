# minishell
A small shell

TODOLIST:
	PARSING:
		-separate commands by;
	MAKE COMMANDS WORK:
		NON_BUILTINS:
			-parse path and replace token with path of executable
		BUILTINS:
			-build them
	-pipes |
	-redirections: <, >, >>
	-$?
	-ctrl-C, ctrl-D and ctrl-\
